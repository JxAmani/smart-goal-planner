{
  "name": "smart-goal-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
