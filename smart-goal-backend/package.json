{
  "name": "smart-goal-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}
